{
    "command": "hadoop",
    "cases":
    [
        {
            "predicate":
            {
                "operator": "exists",
                "operands": ["-cat"]
            },
            "class": "pure",
            "inputs": ["args[1]"],
            "outputs": ["stdout"],
			"comments": "This represents hadoop fs -cat <path>. Slightly hacky since we only check for -cat"
        },
        {
            "predicate": "default",
            "class": "side-effectful",
            "inputs": ["stdin"],
            "outputs": ["stdout"]
        }
    ]
}
