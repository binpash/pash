{
    "command": "xargs",
    "cases":
    [
        {
            "predicate": "default",
            "class": "side-effectful",
            "inputs": ["stdin"],
            "outputs": ["stdout"],
            "options": ["resolve_at_runtime"],
			"comment": "In most conservative category. Resolve the arguments at runtime recursively and overwrite the class."
        }
    ]
}
