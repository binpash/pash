{
    "command": "gunzip",
    "cases":
    [
        {
            "predicate": "default",
            "class": "non-pure",
            "inputs": ["stdin"],
            "outputs": ["stdout"]
        }
    ]
}
