{
    "command": "cat",
    "cases":
    [
	{
            "predicate":
            {
                "operator": "or",
                "operands": [
                    {
                        "operator": "exists",
                        "operands": ["-b"]
                    },
                    {
                        "operator": "exists",
                        "operands": ["-n"]
                    },
                    {
                        "operator": "exists",
                        "operands": ["-s"]
                    }
                ]
            },
            "aggregate": "TODO",
            "class": "pure",
            "inputs": ["args[:]"],
            "outputs": ["stdout"],
			"comment": "Pure because -n and -b prints line numbering, and splitting affects it. -s squeezes repeated empty lines so if split in the middle of empty lines, `cat` is no longer enough to aggregate."
	},
        {
            "predicate": "default",
            "class": "stateless",
            "inputs": ["args[:]"],
            "outputs": ["stdout"]
        }
    ],
    "options": ["stdin-hyphen"]
}
