{
    "command": "test_one",
    "cases":
    [
        {
            "predicate": "default",
            "class": "parallelizable_pure",
            "inputs": ["stdin"],
            "outputs": ["stdout"],
            "aggregator":
            {
                "path": "runtime/agg/opt/concat.sh"
            }
        }
    ]
}
